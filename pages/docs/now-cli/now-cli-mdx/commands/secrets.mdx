import Snippet from '~/components/snippet'
import Caption from '~/components/text/caption'
import { InlineCode } from '~/components/text/code'

export const meta = {
  editUrl: 'pages/docs/now-cli/now-cli-mdx/commands/secrets.mdx',
  lastEdited: '2019-10-15T22:03:42.000Z'
}

## Secrets

The `now secrets` command is used to manage [Now Secrets](/docs/v2/serverless-functions/env-and-secrets/#adding-secrets) under an account, providing functionality to list, add, rename, and remove Secrets.

### Basic Usage

<Snippet dark text="now secrets list" />
<Caption>Using the <InlineCode>now secrets</InlineCode> command to list all Now Secrets under an account.</Caption>

### Extended Usage

<Snippet dark text="now secrets add [secret-name] [secret-value]" />
<Caption>Using the <InlineCode>now secrets</InlineCode> command to add a Now Secret to an account.</Caption>

<Snippet dark text="now secrets rename [old-name] [new-name]" />
<Caption>Using the <InlineCode>now secrets</InlineCode> command to rename a Now Secret under an account.</Caption>

<Snippet dark text="now secrets remove [secret-name]" />
<Caption>Using the <InlineCode>now secrets</InlineCode> command to remove a Now Secret from an account.</Caption>

### Global Options

The following [global options](#options) can be passed when using the `now secrets` command:

- `--debug`
- `--global-config`
- `--help`
- `--local-config`
- `--scope`
- `--token`

For more information on global options and their usage, refer to the [options section](#options).
