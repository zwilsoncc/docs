import Snippet from '~/components/snippet'
import Caption from '~/components/text/caption'
import { InlineCode } from '~/components/text/code'

export const meta = {
  editUrl: 'pages/docs/now-cli/now-cli-mdx/commands/logout.mdx',
  lastEdited: '2019-10-15T22:03:42.000Z'
}

## Logout

The `now logout` command allows you to logout of your ZEIT account through Now CLI.

### Basic Usage

<Snippet dark text="now logout" />
<Caption>Using the <InlineCode>now logout</InlineCode> command to logout of a ZEIT account.</Caption>
