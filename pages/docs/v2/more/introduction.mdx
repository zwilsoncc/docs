import Doc from '~/components/layout/docs'
import { Image } from '~/components/media'
import Caption from '~/components/text/caption'
import Note from '~/components/text/note'
import { Code } from '~/components/text/code'
import Card from '~/components/card'
import Link from '~/components/text/link'

export const meta = {
  title: 'More Features',
  description: 'More features from ZEIT Now to extend your usage.',
  editUrl: 'pages/docs/v2/more/introduction.mdx',
  lastEdited: '2020-01-24T23:33:43.000Z'
}

The following is a list of documentation on more features for ZEIT Now.

These features allow you integrate your deployments closer with ZEIT Now and extend the functionality provided by the platform.

<Card title="Deploy Button" href="/docs/v2/more/deploy-button">
  Use a Deploy Button to allow others to deploy your Git repositories.
</Card>

<Card title="Deploy Hooks" href="/docs/v2/more/deploy-hooks">
  Learn how to trigger deployments from external sources using a Deploy Hook.
</Card>

export default ({ children }) => <Doc meta={meta}>{children}</Doc>

export const config = {
  amp: 'hybrid'
}
