import Errors from '~/components/layout/errors'
import { PRODUCT_NAME } from '~/lib/constants'

export const meta = {
  title: `Inviting Users to Team Who Requested Access`,
  description: `Errors on ${PRODUCT_NAME}`,
  editUrl:
    'pages/docs/errors/teams/inviting-users-to-team-who-requested-access.mdx',
  lastEdited: '2020-05-06T04:38:20.000Z'
}

export default ({ children }) => <Errors meta={meta}>{children}</Errors>

export const config = {
  amp: 'hybrid'
}
