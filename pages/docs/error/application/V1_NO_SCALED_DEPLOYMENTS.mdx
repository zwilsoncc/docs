import Standard from '~/components/layout/standard'
import Note from '~/components/text/note'
import { InlineCode } from '~/components/text/code'
import Link from '~/components/text/link'
import ProductNameV1 from '~/components/product-name/v1'
import { PRODUCT_V1 } from '~/lib/constants'

export const meta = {
  title: `${PRODUCT_V1} No Scaled Deployments`,
  description:
    'The application recieved more traffic than the scale settings accomodate.',
  editUrl: 'pages/docs/error/application/V1_NO_SCALED_DEPLOYMENTS.mdx',
  lastEdited: '2020-04-12T23:13:01.000Z'
}

#### Application Error

An error occurred with the application because it received more traffic than the current scale settings could handle.

<br />

<Note type="warning">
  This error is from <ProductNameV1 />.{' '}
  <Link href="https://zeit.co/guides/migrate-to-zeit-now">
    Upgrade to the latest platform version
  </Link>{' '}
  to have automatic scaling for your applications along with many more benefits
  including blazing-fast speeds plus an automatic HTTPS and global CDN.
</Note>

<br />

<Note label={false}>
  <b>503</b>: Service Unavailable
  <br />
  Code: <InlineCode>V1_NO_SCALED_DEPLOYMENTS</InlineCode>
</Note>

---

#### Troubleshoot

- Try increasing the scale settings on your application, for example:
  ```
  // scale up to 10 instances
  now scale my-deployment-my-username.now.sh 10
  ```
- For more help try `now scale --help` or check out the [<ProductName /> Scaling Documentation](/docs/v1/getting-started/scaling).

export default ({ children }) => <Standard meta={meta}>{children}</Standard>

export const config = {
  amp: 'hybrid'
}
