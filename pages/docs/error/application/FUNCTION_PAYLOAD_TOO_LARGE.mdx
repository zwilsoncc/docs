import Standard from '~/components/layout/standard'
import Note from '~/components/text/note'
import { InlineCode } from '~/components/text/code'

export const meta = {
  title: 'Function Payload Too Large',
  description:
    'The request\'s body to the function is too large.',
  editUrl: 'pages/docs/error/application/FUNCTION_PAYLOAD_TOO_LARGE.mdx',
  lastEdited: '2019-10-07T13:41:14.000Z'
}

#### Application Error

An error occurred in the application when requesting a Serverless Function with a payload exceeding `6MB`.

<br />

<Note label={false}>
  <b>413</b>: Request Entity Too Large
  <br />
  Code: <InlineCode>FUNCTION_PAYLOAD_TOO_LARGE</InlineCode>
</Note>

---

#### Troubleshoot

- Reduce the size of the request payload.

export default ({ children }) => <Standard meta={meta}>{children}</Standard>

export const config = {
  amp: 'hybrid'
}
