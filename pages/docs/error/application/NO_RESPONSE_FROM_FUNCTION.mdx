import Standard from '~/components/layout/standard'
import Note from '~/components/text/note'
import { InlineCode } from '~/components/text/code'

export const meta = {
  title: 'No Response from Serverless Function',
  description:
    'The requested Serverless Function failed to respond with a status code.',
  editUrl: 'pages/docs/error/application/NO_STATUS_CODE_FROM_FUNCTION.mdx',
  lastEdited: '2020-05-13T21:43:00.000Z'
}

#### Application Error

The application did not respond with a status code.

<br />

<Note label={false}>
  <b>502</b>: Bad Gateway
  <br />
  Code: <InlineCode>NO_RESPONSE_FROM_FUNCTION</InlineCode>
</Note>

---

#### Troubleshoot

- Make sure the Serverless Function from the application responds with a status code.
- Check the application logs, which can be found at the host URL under [the `/_logs` path](/docs/v2/platform/deployments/#logs-view), for example:
  ```
  https://my-deployment-my-username.now.sh/_logs
  ```

export default ({ children }) => <Standard meta={meta}>{children}</Standard>

export const config = {
  amp: 'hybrid'
}
