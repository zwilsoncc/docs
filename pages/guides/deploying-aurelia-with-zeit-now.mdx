import Guide from '~/components/layout/guide'
import Snippet from '~/components/snippet'
import Caption from '~/components/text/caption'
import Link from '~/components/text/link'
import { Image } from '~/components/media'
import { InlineCode } from '~/components/text/code'

export const meta = {
  title: 'Deploy Aurelia with ZEIT Now',
  description: 'Create an Aurelia app and deploy it live with ZEIT Now.',
  published: '2019-11-01T12:00:00.860Z',
  authors: ['msweeneydev'],
  url: '/guides/deploying-aurelia-with-zeit-now',
  example: 'aurelia',
  demo: 'https://aurelia.now-examples.now.sh',
  image:
    'https://og-image.now.sh/**Deploy%20Aurelia%20Apps**%20%3Cbr%2F%3E%20with%20ZEIT%20Now.png?theme=light&md=1&fontSize=100px&images=https%3A%2F%2Fassets.zeit.co%2Fimage%2Fupload%2Ffront%2Fassets%2Fdesign%2Fzeit-black-triangle.svg&images=https%3A%2F%2Fcdn.svgporn.com%2Flogos%2Faurelia.svg&widths=300&widths=300&heights=300&heights=300',
  editUrl: 'pages/guides/deploying-aurelia-with-zeit-now.mdx',
  lastEdited: '2019-11-05T13:14:41.000Z'
}

[Aurelia](https://aurelia.io/home) is a JavaScript framework for web, mobile and desktop that leverages simple conventions to empower your creativity.

## Step 1: Set Up Your Aurelia Project

Set up an Aurelia app with [`npx`](https://www.npmjs.com/package/npx) and move into the project directory, choosing the option **Default ESNext App**:

<Snippet dark text="npx aurelia-cli new && cd aurelia-app" />
<Caption>Initializing an Aurelia app with <Link href="https://www.npmjs.com/package/npx">npx</Link> and moving into the project directory.</Caption>

In the `./aurelia_project/aurelia.json` file, under the `platform` key, change the output directory for the project to `public`:

```diff
"platform": {
    "hmr": false,
    "open": false,
    "port": 8080,
-   "output": "dist"
+   "output": "public"
}
```

<Caption>
  Changing the output directory in the{' '}
  <InlineCode>/aurelia_project/aurelia.json</InlineCode> file.
</Caption>

## Step 2: Deploying Your Aurelia App with ZEIT Now

With an Aurelia app set up, it is ready to deploy live with [Now](/docs/v2).

Using [Now CLI](/download), deploy your app with **a single command**:

<Snippet dark text="now" />
<Caption>Deploying your Aurelia app with Now CLI.</Caption>

If you want to deploy your Aurelia app when you push to a Git repository, you can use either [Now for GitHub](/docs/v2/integrations/now-for-github/) or [Now for GitLab](/docs/v2/integrations/now-for-gitlab/) to have your project automatically deployed on every push, and the [production domain](https://zeit.co/docs/v2/custom-domains/#adding-a-domain) updated on push to master.

export default ({ children }) => <Guide meta={meta}>{children}</Guide>

export const config = {
  amp: 'hybrid'
}
