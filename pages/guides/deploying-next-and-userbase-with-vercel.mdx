import Guide from '~/components/layout/guide'
import { Code, InlineCode } from '~/components/code'
import Snippet from '~/components/snippet'
import Caption from '~/components/text/caption'
import Link from '~/components/text/link'
import { Image } from '~/components/media'
import DeploySection from '~/components/guides/deploy-section'
import NameWrapper from '~/components/name/name-wrapper'
import { PRODUCT_NAME } from '~/lib/constants'

export const meta = {
  title: `Deploy Next.js and Userbase with ${PRODUCT_NAME}`,
  description: `Create a todo app with Next.js and Userbase and deploy it live with ${PRODUCT_NAME}.`,
  published: '2020-04-23T12:00:00.860Z',
  authors: ['coetry'],
  url: '/guides/deploying-next-and-userbase-with-vercel',
  demo: 'https://next-userbase.now-examples.now.sh',
  image: '',
  editUrl: 'pages/guides/deploying-next-and-userbase-with-vercel.mdx',
  lastEdited: '2020-04-23T22:22:40.000Z'
}

[Userbase](https://userbase.com/) is the easiest way to add user accounts and data persistence to your static site.
All Userbase features are accessible through a [JavaScript SDK](https://userbase.com/docs/sdk/), directly from the browser, without requring a backend.

## Step 1: Create A Userbase Account

First you'll need to create a Userbase account if you don't already have one. You can create one in _less than 10 seconds_ [here](https://v1.userbase.com/#create-admin).

After creating your account, make note of your _App ID_. We'll store it later as an environment variable for our project.

## Step 2: Set Up A New Next.js Project

Let's start off by scaffolding a Next.js `with-userbase` template from the [Next.js Examples Directory](https://github.com/zeit/next.js/tree/canary/examples/with-userbase)

<Snippet dark text="npm init next-app --example with-userbase userbase-todo-app && cd userbase-todo-app" />
<Caption>Initializing a Next.js + Userbase app and entering the new project directory</Caption>

## Step 3: Add Environment Variables

The scaffolded template expects us to provide a `USERBASE_APP_ID` variable as shown in `next.config.js`.

<Code lang="js">
  {`
    module.exports = {
      env: {
        USERBASE_APP_ID: process.env.USERBASE_APP_ID,
      },
    };
`}
</Code>
<Caption><InlineCode>USERBASE_APP_ID</InlineCode> environment variable in next.config.js</Caption>

For development, you can simply add your Userbase _App ID_ from _Step 1_ as an environment variable in a `.env` file at the project root and run `next dev` to start a development server.

<Code lang="bash">
  {`USERBASE_APP_ID=<paste-your-id-here>`}
</Code>
<Caption>Defining the <InlineCode>USERBASE_APP_ID</InlineCode> environment variable in a <InlineCode>.env</InlineCode> file.</Caption>

In order to take your app to production, you can add the _App ID_ as a secret by running the following command:

<Snippet dark text="now secrets add userbase-app-id [paste-your-id-here]" />
<Caption>Adding a secret to your Vercel account.</Caption>

The project template already provides you with a `now.json` file that makes use of the secret we added above.

<Code lang="json">
  {`
    {
      "build": {
        "env": {
          USERBASE_APP_ID: "@userbase-app-id"
        }
      }
    }
  `}
</Code>
<Caption>The <InlineCode>now.json</InlineCode> file that provides the defined secret to the application.</Caption>

## Step 4: Deploying To Production

<DeploySection meta={meta} />

export default ({ children }) => <Guide meta={meta}>{children}</Guide>

export const config = {
  amp: 'hybrid'
}
