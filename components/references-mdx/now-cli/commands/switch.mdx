import Snippet from '~/components/snippet'
import Caption from '~/components/text/caption'
import { InlineCode } from '~/components/text/code'
import ProductShortName from '~/components/name/product-short-name'

export const meta = {
  editUrl: 'pages/docs/now-cli/now-cli-mdx/commands/switch.mdx',
  lastEdited: '2019-10-15T22:03:42.000Z'
}

## Switch

The `now switch` command is used to switch to a different team scope when logged in with <ProductShortName /> CLI. You can choose to select a team from a list of all those you are part of or specify a team when entering the command.

### Basic Usage

<Snippet dark text="now switch" />
<Caption>Using the <InlineCode>now switch</InlineCode> command to change team scope with <ProductShortName /> CLI.</Caption>

### Extended Usage

<Snippet dark text="now switch [team-name]" />
<Caption>Using the <InlineCode>now switch</InlineCode> command to change to a specific team scope with <ProductShortName /> CLI.</Caption>

### Global Options

The following [global options](#options) can be passed when using the `now switch` command:

- `--debug`
- `--help`

For more information on global options and their usage, refer to the [options section](#options).
