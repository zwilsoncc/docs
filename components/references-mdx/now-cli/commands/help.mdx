import Snippet from '~/components/snippet'
import Caption from '~/components/text/caption'
import { InlineCode } from '~/components/text/code'
import ProductShortName from '~/components/name/product-short-name'

export const meta = {
  editUrl: 'pages/docs/now-cli/now-cli-mdx/commands/help.mdx',
  lastEdited: '2019-10-15T22:03:42.000Z'
}

## Help

The `now help` command generates a list of all available <ProductShortName /> CLI [commands](#commands) and [options](#options) in the terminal. When combined with a second argument - a valid <ProductShortName /> CLI [command](#commands) - it outputs more detailed information about that command.

### Basic Usage

<Snippet dark text="now help" />
<Caption>Using the <InlineCode>now help</InlineCode> command to generate a list of <ProductShortName /> CLI commands and options.</Caption>

### Extended Usage

<Snippet dark text="now help [command]" />
<Caption>Using the <InlineCode>now help</InlineCode> command to generate detailed information about a specific <ProductShortName /> CLI command.</Caption>
