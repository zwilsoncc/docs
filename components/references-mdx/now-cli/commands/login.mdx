import Snippet from '~/components/snippet'
import Caption from '~/components/text/caption'
import { InlineCode } from '~/components/text/code'
import ProductNameSimple from '~/components/product-name/simple'

export const meta = {
  editUrl: 'pages/docs/now-cli/now-cli-mdx/commands/login.mdx',
  lastEdited: '2019-10-15T22:03:42.000Z'
}

## Login

The `now login` command allows you to login to your <ProductNameSimple /> account through Now CLI.

### Basic Usage

<Snippet dark text="now login" />
<Caption>Using the <InlineCode>now login</InlineCode> command to login to a <ProductNameSimple /> account.</Caption>
