import Snippet from '~/components/snippet'
import Caption from '~/components/text/caption'
import { InlineCode } from '~/components/text/code'
import ProductShortName from '~/components/name/product-short-name'

export const meta = {
  lastEdited: '2019-10-15T22:03:42.000Z'
}

## Help

The `vercel help` command generates a list of all available <ProductShortName /> CLI [commands](#commands) and [options](#options) in the terminal. When combined with a second argument - a valid <ProductShortName /> CLI [command](#commands) - it outputs more detailed information about that command.

### Basic Usage

<Snippet dark text="vercel help" />
<Caption>Using the <InlineCode>vercel help</InlineCode> command to generate a list of <ProductShortName /> CLI commands and options.</Caption>

### Extended Usage

<Snippet dark text="vercel help [command]" />
<Caption>Using the <InlineCode>vercel help</InlineCode> command to generate detailed information about a specific <ProductShortName /> CLI command.</Caption>
