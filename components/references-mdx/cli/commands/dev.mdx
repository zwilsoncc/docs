import Snippet from '~/components/snippet'
import Caption from '~/components/text/caption'
import { InlineCode } from '~/components/text/code'
import Note from '~/components/text/note'
import ProductName from '~/components/name/product-name'

export const meta = {
  lastEdited: '2019-10-15T22:03:42.000Z'
}

## Dev

The `vercel dev` command is used to replicate the <ProductName /> deployment environment locally, allowing you to test your [Serverless Functions](/docs/v2/serverless-functions/introduction), without requiring you to deploy each time a change is made.

<Note>
  Before using the <InlineCode>vercel dev</InlineCode> command, make sure to
  install your project's dependencies.
</Note>

### Basic Usage

<Snippet dark text="vercel dev" />
<Caption>Using the <InlineCode>vercel dev</InlineCode> command from the root of a project directory.</Caption>

### Global Options

The following [global options](#options) can be passed when using the `vercel dev` command:

- `--debug`
- `--help`

For more information on global options and their usage, refer to the [options section](#options).

### Unique Options

These are options that only apply to the `vercel dev` command, therefore, more information is provided.

#### Listen

The `--listen` option, shorthand `-l`, can be used to specify which port `vercel dev` runs on.

##### Usage Example

<Snippet dark text="vercel dev --listen 5005" />
<Caption>Using the <InlineCode>vercel dev</InlineCode> command with the <InlineCode>--listen</InlineCode> option.</Caption>
