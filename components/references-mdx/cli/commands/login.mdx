import Snippet from '~/components/snippet'
import Caption from '~/components/text/caption'
import { InlineCode } from '~/components/text/code'
import OrgName from '~/components/name/org-name'
import ProductShortName from '~/components/name/product-short-name'

export const meta = {
  lastEdited: '2019-10-15T22:03:42.000Z'
}

## Login

The `vercel login` command allows you to login to your <OrgName /> account through <ProductShortName /> CLI.

### Basic Usage

<Snippet dark text="vercel login" />
<Caption>Using the <InlineCode>vercel login</InlineCode> command to login to a <OrgName /> account.</Caption>
