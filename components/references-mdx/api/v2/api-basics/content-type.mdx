import ProductName from '~/components/name/product-name'

export const meta = {
  editUrl: 'pages/docs/api/v2/api-docs-mdx/api-basics/content-type.mdx',
  lastEdited: '2019-01-03T18:58:18.000Z'
}

## Content Type

All requests must be encoded as JSON with the `Content-Type: application/json` header.
If not otherwise specified, responses from the <ProductName /> API, including errors, are encoded exclusively as JSON as well.
