import Example from '~/components/example'
import { Code } from '~/components/text/code'

export const meta = {
  editUrl: 'pages/docs/api/v1/api-docs-mdx/errors/oauth2.mdx',
  lastEdited: '2019-01-04T22:52:33.000Z'
}

## OAuth2 Errors

These errors could occur when using any [OAuth2 related endpoint](#endpoints/oauth2).

### Client Not Found

The OAuth2 client ID could not be found or doesn't exists.

<Example>
  <Code lang="json">{`{
    "error": {
      "code": "not_found",
      "message": "OAuth client doesn't not found: CLIENT_ID"
    }
  }`}</Code>
</Example>
