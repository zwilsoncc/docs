import Card from '~/components/card'
import { Code, InlineCode } from '~/components/text/code'
import { P } from '~/components/text'
import Link from '~/components/text/link'
import Note from '~/components/text/note'
import ProductName from '~/components/name/product-name'

export const meta = {
  editUrl:
    'pages/docs/configuration/configuration-mdx/git-integrations/git-integrations.mdx',
  lastEdited: '2019-10-17T14:44:04.000Z'
}

# Git Integrations

The following configuration options can be used through a `now.json` file like the [options above](#configuration).

## github.enabled

**Type**: `Boolean`.

When set to `false`, [<ProductName /> for GitHub](/docs/v2/git-integrations/vercel-for-github) will not deploy the given project regardless of the GitHub app being installed.

<Code lang="json">{`{
  "github": {
    "enabled": false
  }
}`}</Code>

## github.autoAlias

**Type**: `Boolean`.

When set to `false`, [<ProductName /> for GitHub](/docs/v2/git-integrations/vercel-for-github) will not apply the alias upon merge.

<Code lang="json">{`{
  "alias": ["example.com"],
  "github": {
    "autoAlias": false
  }
}`}</Code>

## github.silent

**Type**: `Boolean`.

When set to `true`, [<ProductName /> for GitHub](/docs/v2/git-integrations/vercel-for-github) will stop commenting on pull requests and commits.

<Code lang="json">{`{
  "github": {
    "silent": true
  }
}`}</Code>

## github.autoJobCancelation

**Type**: `Boolean`.

When set to false, [<ProductName /> for GitHub](/docs/v2/git-integrations/vercel-for-github) will always build pushes in sequence without cancelling a build for the most recent commit.

<Code lang="json">{`{
  "github": {
    "autoJobCancelation": false
  }
}`}</Code>
